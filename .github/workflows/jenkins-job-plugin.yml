name: jenkins-job-plugin
on:
  # Triggers the workflow on push or pull request events but only for the "develop" branch
  #push:
  #  branches: [ "develop" ]
  #pull_request:
  #  branches: [ "develop" ]
  
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  your_job_name:
    name: Trigger Jenkins Job
    runs-on: ubuntu-latest
    steps:
      - name: Trigger your-awesome-job-name job
        uses: toptal/jenkins-job-trigger-action@master
        with:
          jenkins_url: "http://20.106.205.93:8080/"
          jenkins_user: "coates"
          jenkins_token: "11769af66b70d9a6e8e7ea1a76d0139c3d"
          proxy: "http://20.106.205.93:8080/"
          job_name: "fake-server-pipeline"
          job_timeout: "3600" # Default 30 sec. (optional)
